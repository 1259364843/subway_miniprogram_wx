<!-- 自定义的tabbar -->
<t-tab-bar value="{{tabBarValue}}" bindchange="onTabBarChange" shape="round" theme="tag" split="{{false}}" fixed="{{false}}">
  <t-tab-bar-item wx:for="{{list}}" wx:key="index" value="{{item.value}}" icon="{{item.icon}}">
    {{item.label}}
  </t-tab-bar-item>
</t-tab-bar>
